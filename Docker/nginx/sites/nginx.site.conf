server {
    listen 80;
    listen [::]:80;
    server_name _;

    charset utf-8;
    client_max_body_size 75M;

    location / {
        root /usr/share/nginx/html/;
        index main.html;
    }
}
